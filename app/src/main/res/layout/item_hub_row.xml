<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="32dp"
    android:paddingBottom="12dp"
    android:clipChildren="false"
    android:clipToPadding="false">

    <TextView
        android:id="@+id/row_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/hub_row_header_margin_start"
        android:layout_marginBottom="8dp"
        android:textColor="@color/text_white"
        android:textSize="20sp"
        android:fontFamily="sans-serif-black"
        android:letterSpacing="0.02"
        android:alpha="0.85" />

    <androidx.leanback.widget.HorizontalGridView
        android:id="@+id/row_grid"
        android:layout_width="match_parent"
        android:layout_height="@dimen/hub_row_grid_height"
        android:clipToPadding="false"
        android:clipChildren="false"
        android:paddingStart="@dimen/hub_row_padding_horizontal"
        android:paddingEnd="@dimen/hub_row_padding_horizontal" />

</LinearLayout>
